{{form_start(form)}}

<table align="center" class="table">
    <tr>
        <th>{{form_label(form.nomLong)}}</th>
        <td>{{form_widget(form.nomLong)}}</td>
    </tr>
    <tr>
        <th>{{form_label(form.nomCourt)}}</th>
        <td>{{form_widget(form.nomCourt)}}</td>
    </tr>
</table>

<div class="" style ="text-align:center">

    <button class="d-none d-sm-inline-block btn btn-sm btn-dark shadow-sm">{{ button_label|default("Créer la formation") }}</button>
    <button onclick="location.href='{{ path('formation_index') }}'" class="d-none d-sm-inline-block btn btn-sm btn-dark shadow-sm">Annuler</button>
</div>


{{form_end(form)}}
